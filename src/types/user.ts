export interface User {
    name: string;
    email: string;
    phone: string;
}

export interface SavedTraveller {
    id: string;
    title: string;
    firstName: string;
    lastName: string;
    dateOfBirth: string;
    gender: string;
    nationality: string;
}
